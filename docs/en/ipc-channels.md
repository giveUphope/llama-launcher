# IPC channel inventory

> Language: English · [中文](../zh/ipc-channels.md)
> Scope: the complete IPC channel inventory (56 channels in total), grouped by category. Read this before changing IPC. The single source of truth for the constants is `packages/shared/src/types/ipc.ts`; the preload-side constants are generated by `scripts/generate-preload.cjs` (run `pnpm generate:ipc` after editing), and `scripts/verify-ipc-sync.cjs` checks during lint that the generated artifact is not stale.
> Index: [README.md](../../README.md) · Related: [desktop-main.md](desktop-main.md)

56 IPC channels in total, grouped by category as follows:

### Settings (2)

| Channel         | Purpose        |
| --------------- | ---- |
| `settings:load` | Load settings  |
| `settings:save` | Save settings  |

### Models (7)

| Channel               | Purpose                                                                                     |
| --------------------- | --------------------------------------------------------------------------------------------- |
| `models:scan`         | Scan the model directory                                                                    |
| `models:detectMmproj` | Detect multimodal projector files                                                           |
| `models:detectDraft`  | Detect draft model files (dflash/draft)                                                     |
| `models:readGgufMeta` | Read GGUF metadata                                                                          |
| `models:watch`        | Watch `.gguf` file changes recursively                                                      |
| `models:remove`       | Remove by model file (when the directory still holds other quantized versions/files, only the selected file is deleted; otherwise the mmproj/mtp/dflash companion files and the now-empty directory are removed too; only paths inside `models\_dir` may be deleted; associated presets are cleaned up in sync) |
| `models:changed`      | Model-change notification (main process → renderer)                                         |

### Presets (4)

| Channel          | Purpose               |
| ---------------- | ------ |
| `presets:list`   | List all presets      |
| `presets:save`   | Save a preset         |
| `presets:load`   | Load a preset         |
| `presets:delete` | Delete a preset       |

### Server (6)

| Channel          | Purpose                                         |
| ---------------- | ----------------------------------------------- |
| `server:start`   | Start the service                               |
| `server:stop`    | Stop the service                                |
| `server:restart` | Restart the service                             |
| `server:status`  | Query service status                            |
| `server:preview` | Preview the launch command                      |
| `server:output-batch` | Output push (main process → renderer), **payload is an `OutputEntry[]` array**: lines within a 16ms window are merged into a single message |

### Logs (3)

| Channel      | Purpose                          |
| ------------ | ------------------------------ |
| `logs:list`  | Read the application log (distinct from the service console: this one records the app's own lifecycle/operations) |
| `logs:clear` | Clear the application log      |
| `logs:onlog` | Application log push (main process → renderer) |

### General (3)

| Channel           | Purpose                        |
| ----------------- | ------------ |
| `clipboard:write` | Write to the clipboard         |
| `open:external`   | Open a link in the system browser |
| `open:path`       | Open a path in the system file manager |

### Window (8)

| Channel                    | Purpose                                                              |
| -------------------------- | ------------------------------------------------------------------ |
| `window:close`             | Close the window                                                   |
| `window:minimize`          | Minimize the window                                                |
| `window:toggleMaximize`    | Toggle maximize/restore                                            |
| `window:state`             | Query window state                                                 |
| `window:maximized`         | Maximized notification (main process → renderer)                   |
| `window:unmaximized`       | Restored notification (main process → renderer)                    |
| `window:showCloseDialog`   | Close-window prompt request (main process → renderer, an in-app dialog replaces the native dialog; payload `CloseDialogRequest`) |
| `window:closeDialogResult` | Close-window prompt reply (renderer → main process; payload `CloseDialogResult`) |

### System (11)

| Channel               | Purpose                                                                                                                  |
| --------------------- | -------------------------------------------------------------------------------------------------------------------- |
| `system:checkPort`    | Check whether the port is taken (probed at the `--host` address llama-server is about to bind; default 127.0.0.1, and for 0.0.0.0/LAN IPs the corresponding address — measured 2026-09: when the holder binds a LAN IP, probing 127.0.0.1 misses while probing that address hits, which covers holders on other network interfaces; on Windows a wildcard and a loopback binding can coexist, so the probe is best effort; when occupied it returns the holder's PID/process name) |
| `system:killProcess`  | Terminate the specified process (port-conflict handling: Windows `taskkill /F /PID`, POSIX SIGKILL; invoked only after the renderer side has confirmed) |
| `system:findFreePort` | Scan forward from the given port and return the first free one (host semantics identical to checkPort; returns null when none is available) |
| `system:estimateVram` | VRAM probe + context capacity estimate (spawns `llama-server --list-devices` to get free VRAM per device + the GGUF KV memory model to estimate the full-offload context ceiling + performance-target linked suggestions; the exe used for probing is resolved by core's `resolveServerExe` with stepwise fallback (`server_exe` → same directory → `llama_dir` and its first-level subdirectories → dev-mode `llama-*-bin-*`), so renaming or moving the engine directory does not fail silently; best effort — failed fields are null and the reason comes back on `probeError` (including the paths that were tried); the 30s device-probe cache **stores successful results only**, resetting the timestamp on failure so a corrected directory re-probes immediately; results are cached for 60s keyed by model\|dtype\|target\|ngl\|ctxSize) |
| `system:benchLlamaRun`    | Start the llama-bench offline health check (pp512/tg128 full offload, fire-and-forget, one job per model; results cached for the session by model path) |
| `system:benchLlamaStatus` | Poll the health-check job status/result (running/done/error + LlamaBenchSummary) |
| `system:estimateModelFit` | Batch VRAM-fit verdict for the model list (fit/partial/no badge + the full-offload context ceiling; verdict is null when the metadata cannot be read) |
| `system:fileExists`   | Check whether a file exists                                                                                          |
| `system:findLlamaExe` | Find the llama-server executable in a directory (inline detection)                                                   |
| `system:detectTrash`  | Detect cleanable items among app-generated files (dual-root scan of the config directory + the model directory; paths of active/paused/retryable download tasks are protected automatically) |
| `system:cleanTrash`   | Perform the cleanup (each item is re-validated for root ownership, kind signature, the protected set and symlinks)  |

### FS (2)

| Channel        | Purpose                 |
| -------------- | ------ |
| `fs:listDir`   | List directory contents |
| `fs:mkdir`     | Create a directory      |

### Download (10)

| Channel              | Purpose                |
| -------------------- | ------------------- |
| `download:parseUrl`  | Parse a model URL     |
| `download:search`    | Search ModelScope models |
| `download:listFiles` | List the files of a model repository |
| `download:start`     | Start a download      |
| `download:cancel`    | Cancel a download (deletes the partial file and the event log) |
| `download:pause`     | Pause a download (keeps the partial file and the event log) |
| `download:resume`    | Resume a download (including retrying a task in error state) |
| `download:progress`  | Download progress push (main process → renderer) |
| `download:complete`  | Download completion push (main process → renderer) |
| `download:error`     | Download error push (main process → renderer) |

